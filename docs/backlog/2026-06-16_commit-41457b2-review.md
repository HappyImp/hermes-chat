# 裁判君审查报告 — commit 41457b2

- 日期：2026-06-16
- 审查范围：51 文件（4140 insertions, 1483 deletions）
- 模块：后端 kanban/permission/admin/auth/tenant、前端 EmployeeStatus/useKanbanWebSocket/employeeMapping/kanban API、文档

---

## 🔍 总评

代码质量：⭐⭐⭐⭐☆ (4/5)
可合并性：❌ 需修改后重审（测试失败）

---

## ✅ 编译验证

- cargo check: ✅ 0 错误
- npx tsc --noEmit: ✅ 0 错误

---

## ✅ 文档完整性

- [x] docs/prd/ — 有相关 PRD（websocket-realtime-update.md 等）
- [x] docs/design/ — admin-panel-design.md 已更新（v2.0, 更新 2026-06-16）
- [x] docs/prd/README.md — 索引存在
- [x] docs/design/README.md — 索引存在
- [x] docs/test/ — 测试报告存在（admin-panel-e2e, kan-401-parallel-verification）
- [x] docs/README.md — 总索引已更新
- [x] docs/backlog/ — 各审查报告存在

---

## ✅ 安全性检查

- [x] axum 路由参数全部使用 `:id` 语法（非 `{id}`）✅
- [x] TenantScope 提取器不再直接读取前端 header/query，只信任 middleware 注入 ✅
- [x] WebSocket 连接上限（100）+ ConnectionGuard 自动释放 ✅
- [x] CLI 调用加 30 秒超时 ✅
- [x] SetPermission.validate() 验证输入 ✅
- [x] 无硬编码密钥/Token ✅

---

## 🔴 严重问题（必须修复）

### 🔴1. 后端测试失败 — `test_list_users_with_join`
- 文件：backend/tests/permission_tests.rs:327
- 问题：`list_users` 返回结构从 `allowed_employees` 改为 `tenants`，但测试仍断言 `u["allowed_employees"].is_array()`
- 影响：后端 1 个测试失败（19 passed, 1 failed）
- 修复：更新测试断言，改为检查 `u["tenants"].is_array()`

### 🔴2. 前端测试失败 — `useEmployeeTask.test.ts`（8 个测试）
- 文件：src/hooks/__tests__/useEmployeeTask.test.ts
- 问题：`useEmployeeTask` 重构后（移除 fetchActiveEmployees 依赖，改用直接 API 调用），但测试仍 mock 旧接口
- 影响：前端 3 个测试文件失败，8 个测试失败（383 passed, 8 failed）
- 具体失败：
  - dispatchTask sends POST and returns TaskInfo
  - dispatchTask throws when employee is busy（旧逻辑：前端检查忙碌；新逻辑：后端检查）
  - dispatchTask throws on API failure
  - removeTask removes task from activeTasks
  - polling updates task status to completed
  - polling stops when task status becomes failed/timeout
  - polling stops when maxRetries is reached
- 修复：更新测试 mock，适配新的 API 调用方式（fetch → `/tasks/${taskId}/status`）

### 🔴3. 前端测试失败 — `cronJobs.test.ts`
- 文件：src/api/__tests__/cronJobs.test.ts
- 问题：`cronJobs.ts` 已废弃（内容清空），但测试文件未同步删除/更新
- 修复：删除或标记为 skip

---

## 🟡 中等问题（建议修复）

### 🟡1. 注释 typo — `task.ttenant`
- 文件：backend/src/services/kanban.rs（get_task 方法内）
- 问题：注释写成 "task.ttenant 存在时必须匹配"，多了一个 t
- 修复：改为 "task.tenant 存在时必须匹配"

### 🟡2. PRD 索引未包含 Tenant 管理功能
- 文件：docs/prd/README.md
- 问题：本次新增了 tenant 管理功能（list_tenants, update_user_tenants, 授权码 tenant 映射），但 PRD 索引中无对应条目
- 修复：补充 tenant 管理 PRD 条目或在现有 kanban-migration-plan 中更新

### 🟡3. cronJobs.ts 废弃但未删除
- 文件：src/api/cronJobs.ts
- 问题：文件标记 @deprecated 并清空内容，但仍存在。对应的测试也未清理
- 修复：删除文件和测试，或在本次提交中清理

### 🟡4. WebSocket snapshot 可能无限增长
- 文件：backend/src/handlers/kanban.rs（handle_ws）
- 问题：snapshot HashMap 只在任务被删除时清理。如果任务持续创建但从不删除，snapshot 会无限增长
- 影响：长期运行的 WS 连接可能内存泄漏
- 修复：建议添加 snapshot 大小上限或定期清理过期条目

### 🟡5. BatchSetPermissions.validate() 未验证 tenant 格式
- 文件：backend/src/models/permission.rs
- 问题：SetPermission.validate() 验证了 tenant 长度和字符，但 BatchSetPermissions.validate() 没有
- 修复：在 BatchSetPermissions.validate() 中添加 tenant 格式验证

---

## 🟢 轻微问题（可选优化）

### 🟢1. useKanbanEvents 中 tasksRef 更新模式
- 文件：src/hooks/useKanbanEvents.ts
- 问题：handleWsEvent 中手动创建新数组 `[...current.slice(0, idx), task, ...current.slice(idx + 1)]` 可以用 `current.map(t => t.id === task_id ? task : t)` 更简洁
- 影响：无功能影响，代码风格

### 🟢2. EMPLOYEE_DEFAULTS 与 EMPLOYEE_ALIASES 分离
- 文件：src/hooks/useEmployeeStatus.ts + src/config/employeeMapping.ts
- 问题：员工元数据（角色/头像/任务）定义在 useEmployeeStatus.ts 的 EMPLOYEE_DEFAULTS 中，别名定义在 employeeMapping.ts 的 EMPLOYEE_ALIASES 中。两处都硬编码了员工列表
- 建议：考虑合并到一处维护

### 🟢3. admin service 中 user_ids 的 placeholders 构建
- 文件：backend/src/services/admin.rs（list_users 方法）
- 问题：用 `format!` 构建 SQL IN 子句的 placeholders，虽然用了参数绑定（安全），但代码略显复杂
- 建议：可用 sqlx 的 `query_as` + `WHERE user_id IN (SELECT ...)` 子查询替代

---

## ✨ 亮点

- **tenant 隔离安全性提升**：TenantScope 提取器不再信任前端参数，只从 middleware 注入读取。安全测试覆盖完整（5 个新测试）✅
- **WebSocket 健壮性**：ConnectionGuard RAII 模式自动释放连接计数，心跳机制，连接上限 ✅
- **CLI 超时保护**：kanban list CLI 调用加 30 秒 timeout，避免阻塞 tokio 线程 ✅
- **代码简化**：employeeMapping 从 87 行精简到 38 行，useEmployeeStatus 移除 cron/active 依赖 ✅
- **useKanbanEvents hook**：事件驱动替代 60s 轮询，有降级策略，页面获焦自动刷新 ✅
- **Migration 文件**：结构清晰，有索引和约束，兼容旧数据迁移 ✅
- **Permission 模型增强**：validate() 方法 + 辅助方法 + 充分的单元测试 ✅

---

## 📋 总结

本次变更质量整体不错，架构设计合理，安全性有明显提升。但有 **3 个严重问题**（测试失败）必须修复：

1. 后端 `test_list_users_with_join` 断言未更新
2. 前端 `useEmployeeTask.test.ts` 8 个测试未适配新接口
3. 前端 `cronJobs.test.ts` 未清理

**结论：❌ 需修改后重审**

修复 3 个 🔴 后重新提交审查。
