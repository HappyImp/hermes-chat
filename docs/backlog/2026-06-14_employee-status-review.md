# 员工状态面板审查待优化清单

> 来源：裁判君审查（2026-06-14）
> 代码质量：⭐⭐⭐⭐☆ (4/5)
> 审查结论：✅ 可通过（无严重问题）

## 🟡 中等问题（建议修复）

### 1. 类型断言绕过类型安全 — useEmployeeStatus.ts:5
- **问题**：`as unknown as Employee[]` 双重类型断言跳过编译期检查
- **风险**：JSON 数据结构变更时不会在编译期捕获错误
- **建议**：添加运行时类型校验函数，或添加注释说明断言原因

### 2. ~~刷新逻辑为无操作 — useEmployeeStatus.ts:13~~ ✅ 已修复
- **问题**：`refresh()` 每次设置同一模块级引用，60 秒定时器实际无效
- **修复**：hook 已重写，调用 `fetchCronJobs()` 获取实时数据（通过 cron-bridge 脚本）
- **状态**：2026-06-14 已修复

## 🟢 轻微问题（可选优化）

### 3. 冗余 useCallback 包装 — EmployeeStatus.tsx:51-53
- **问题**：`handleRefresh` 只是调用 `refresh()`，额外包装无收益
- **建议**：直接在 `onClick` 中传 `refresh`

### 4. tasks.map key 潜在冲突 — EmployeeStatus.tsx:36
- **问题**：用 `task` 字符串做 key，同名任务会冲突
- **建议**：`key={`${emp.name}-${task}`}`

### 5. 测试未覆盖空列表边界 — EmployeeStatus.test.tsx
- **问题**：未测试 0 人场景
- **建议**：补充空列表测试

### 6. 文档遗漏 lastUpdated 显示 — docs/components/README.md:91
- **问题**：未说明顶部统计栏显示最后更新时间
- **建议**：补充到特性列表

---

## 修复记录

| 日期 | 问题编号 | 修复人 | 状态 |
|------|---------|--------|------|
| 2026-06-14 | #2 | 404 | ✅ 已修复 |
